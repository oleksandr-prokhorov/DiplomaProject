@page "{login}/{id:int}"
@model QueriesToDBTest.Pages.Client.VisitorApplicationViewModel
@{
    Layout = "./_Visitor_Layout";
}
<a asp-page="VisitorStart" asp-route-login=@Model.Login><img src="~/return.png"  width="64" height="64"></a>
<div class="row">
     <div class="col-md-4">
        <img src="~/application.png" width="128" height="128" >
        <h3>Подача заяви</h3>
        <p> Перейдіть на цю форму, щоб оформити заяву на отримання соціальної допомоги</p>
        <p> <a class="btn btn-primary btn-sm" asp-page="VisitorApplication" asp-route-id=@Model.No asp-route-login=@Model.Login>Подача заяви</a></p>
    </div>
    <div class="col-md-4">
        <img src="~/status.png" width="128" height="128" >
        <h3>Стан заяви</h3>
        <p> Перейдіть на цю форму, щоб перевірити стан поданої заяви</p>
        <p> <a class="btn btn-primary btn-sm" asp-page="VisitorShowApplication" asp-route-id=@Model.No asp-route-login=@Model.Login>Перевірити стан заяви</a></p>
    </div>
</div>

